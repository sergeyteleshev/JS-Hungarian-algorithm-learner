import {connect} from 'react-redux';
import TestRibsTableComponent from '../components/TestRibsTableComponent';

const mapStateToProps = (state) => {
    return {

    }
};

const mapDispatchToProps = (dispatch)=> {
    return {

    }
};

const RibsTableVisible = connect(mapStateToProps, mapDispatchToProps)(TestRibsTableComponent);

export default TestRibsTableComponent;