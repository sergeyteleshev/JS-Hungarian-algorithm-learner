import {connect} from 'react-redux';
import RibsTableComponent from '../components/RibsTableComponent';

const mapStateToProps = (state) => {
    return {

    }
};

const mapDispatchToProps = (dispatch)=> {
    return {

    }
};

const RibsTableVisible = connect(mapStateToProps, mapDispatchToProps)(RibsTableComponent);

export default RibsTableVisible;